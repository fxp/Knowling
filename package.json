{
  "name": "knowling-api-cf",
  "private": true,
  "description": "Cloudflare Worker + Container deploy for the Knowling API",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
