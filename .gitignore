__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/
out/
.DS_Store

# local secrets (API keys) — never commit
.env
*.key

# harness artifacts
.claude/*.lock

# manim toolchain + render artifacts (optional [manim] extra)
.venv-manim/
media/

# demo build artifacts (keep index.html + components, ignore preview shots)
demo/_shot-*.png
